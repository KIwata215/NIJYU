import 'package:flutter/material.dart';

class test_nextPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text("Sign In compleat")),
        body: Center(
          child: Column(
            children: [
              
            ],
          ),
        ),
    );

  }

}